# 🧑‍💻 User Management System (Laravel)

A simple Laravel-based User Management System with authentication, CRUD operations, soft deletes, and restore functionality.

---

## ⚙️ How to Use the App

1. Create `.env` file with database credentials  
2. Run `composer install`  
3. Run `php artisan migrate`  
4. Run `php artisan db:seed`  
5. Run `npm install`  
6. Run `npm run build`  
7. Run `php artisan key:generate`  
8. Run `php artisan storage:link`  

---

## 🚀 Run the Application

Start the local development server:
```bash
php artisan serve

Default Seeded Credentials

You can use the following default credentials to log in after migration and seeding:

Email: admin@example.com
Password: password